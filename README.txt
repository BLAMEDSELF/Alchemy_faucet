# Sepolia Faucet Automation
This script automates requesting Sepolia ETH tokens from the Alchemy faucet using Selenium and CapSolver.

## Requirements
See `requirements.txt`.

## Usage
1. Configure `config.py` with your wallets and CapSolver API key.
2. Run `python main.py`.
